// src/components/PortfolioShell.jsx
import React from 'react';
import NavPanel from './NavPanel';

const PortfolioShell = ({ children }) => {
  return (
    
      
      
        {children}
      
    
  );
};

const styles = {
  shell: {
    display: 'grid',
    gridTemplateColumns: '260px 1fr',
    minHeight: '100vh',
    backgroundColor: '#0b1020',

export default PortfolioShell;
Copy
